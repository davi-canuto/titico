## MODIFIED Requirements

### Requirement: Branding
The login page SHALL display the platform name "Lobby do Titiltei" and match the app's visual identity (dark background, red accent, Shaco theme).

#### Scenario: Login page renders correct brand name
- **WHEN** an unauthenticated user navigates to `/login`
- **THEN** the page displays "Lobby do Titiltei" as the platform name
- **THEN** no reference to "Bar", "Sindicato" or previous names appears on the page
